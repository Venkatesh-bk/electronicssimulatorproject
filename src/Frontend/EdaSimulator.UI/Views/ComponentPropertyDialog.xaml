<Window x:Class="EdaSimulator.UI.Views.ComponentPropertyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Component Properties"
        Width="380" Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E"
        Foreground="White"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#5A5A8A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="Cyan"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#AAAACC"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="0,4,0,2"/>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#5B6EFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7B8EFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
            <Setter Property="Background" Value="#3D3D55"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5D5D75"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Edit Component Properties"
                   FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,16"/>

        <!-- Designator -->
        <Label Grid.Row="1" Content="Designator (e.g. R1, C2, V1)"/>
        <TextBox Grid.Row="2" x:Name="DesignatorBox" Margin="0,0,0,12"/>

        <!-- Value -->
        <Label Grid.Row="3" Content="Value (e.g. 10k, 100nF, DC 5, 1N4148)"/>
        <TextBox Grid.Row="4" x:Name="ValueBox" Margin="0,0,0,4"/>

        <!-- Hint -->
        <TextBlock Grid.Row="5" x:Name="HintBlock" Foreground="#8888AA" FontSize="11" TextWrapping="Wrap"
                   Text="Tip: For voltage sources use 'DC 5' or 'PULSE(0 5 0 1n 1n 5u 10u)'. For resistors use '1k', '10k', '1Meg'."/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelBtn" Content="Cancel" Style="{StaticResource SecBtn}"
                    Margin="0,0,10,0" Click="CancelBtn_Click"/>
            <Button x:Name="ApplyBtn" Content="Apply" Style="{StaticResource PrimaryBtn}"
                    IsDefault="True" Click="ApplyBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
